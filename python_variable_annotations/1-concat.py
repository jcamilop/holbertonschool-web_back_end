#!/usr/bin/env python3
'''concat two string'''


def concat(str1: str, str2: str) -> str:
    """ Function concat, takes a string str1 and a string
        str2 as arguments and returns a concatenated string
    """
    return str(str1 + str2)
